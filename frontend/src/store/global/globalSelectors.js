const getToggleAuthForm = (state) => state.global.toggleForm;
const getFormData = (state) => state.global.formData;
const getErrorMessage = (state) => state.global.error;

export { getToggleAuthForm, getFormData, getErrorMessage };
